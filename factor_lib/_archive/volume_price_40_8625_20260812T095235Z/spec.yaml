name: volume_price_40_8625
version: 0.1.0
status: draft
family: price_volume
category: volume_price
universe: csi100
frequency: daily
required_fields:
- close
- open
- high
- low
lookback: 20
horizon: 5
neutralize: []
params:
  expression: mul(rank(roc(close_adj,40)),rank(roc(vol,40)))
tags:
- dsl
- loop
hypothesis: 量价配合；模板启发表达式 mul(rank(roc(close_adj,40)),rank(roc(vol,40)))
entry_gate: research
expression: mul(rank(roc(close_adj,40)),rank(roc(vol,40)))
mechanism: volume_price
expr_hash: null
