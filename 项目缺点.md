# qfactor 项目缺点与优化方案

- 评估日期：2026-08-14
- 范围：中证 100（CSI100 / 000903）日频量价因子挖掘；LangGraph 循环 `decide → generate → review_validate → persist`
- 库快照：约 139 条因子（draft 43、screened 92、candidate 4、approved 0）；循环约第 582 轮；数据窗口 2024-01-02～2026-06-30
- 口径：对照 `src/qfactor`、`configs/` 与磁盘数据。未新跑回测。方案是设计，不是已落地补丁。

## 结论

流水线骨架可用：DSL、双闸、残差 IC、符号冻结、T+1。  
**当前库不能当已验收的生产因子。** `candidate` 只表示过了本机 production 闸。

优化的目标不是把 IC 做大，而是让过闸标签与声称一致：**点时中证 100、挖掘与晋升同一信号、holdout 不被搜索看见、多重测试后仍站得住。** 做完之后，4 条 candidate 很可能减少而不是增加——这是收益，不是损失。

| 状态 | 现在 | 优化后应变成 |
|---|---|---|
| screened | 92，偏松 research 闸 | 更少、更干净的搜索痕迹，仍禁止进策略 |
| candidate | 4，快照样本 + 平滑信号 + 泄漏 holdout | 点时样本上、同一信号定义、未见 holdout 的复评结果 |
| approved | 0 | 仅人工确认、复评仍过的少数条 |

---

## 优化后整体能换到什么

| 做完 | 能声称 | 仍不能声称 |
|---|---|---|
| P0 数据 + 评测合同对齐 | IC 是点时中证 100、挖掘与晋升可比 | 实盘可赚、容量、冲击 |
| P0 堵泄漏 + 多重检验 | candidate 不是被 holdout 和 582 轮搜出来的幸运值 | 因子「一定有经济含义」 |
| P1 恢复结构搜索 | 有机会出现非振幅家族的新假设 | 保证再打出 N 条 candidate |
| P1 工程卫生 | 同步/评测失败可见、批次可复现 | 替代研究判断 |
| P2 合成层 | 下游能读一个小而干净的库 | 完整多因子产品 |

**不要先做的：** 把 `batch` 加到 100、关掉 `max_per_skeleton`、用单因子 Sharpe 当硬闸、在快照数据上继续空转循环。那些会放大下面所有缺点。

---

## 优先级（打包做，不要 37 条平行改）

### P0 — 让现有 4 条数字变得可解释（约 1～2 周，卡 token）

覆盖：#1 #2 #4 #5 #6 #7 #8 #9，以及复评 4 条 candidate。

1. 配 `TUSHARE_TOKEN`，把 `index_weight`/`index_member`、`daily_basic.circ_mv`、`adj_factor` 真正接入 `sync`。
2. `sync-universe` + 按并集 `sync-data`，刷新 2023-12 调样起的窗口。
3. 挖掘与晋升用同一信号（要么两边都不做 5 日滚动均值，要么挖掘时就做，并写进 spec）。
4. research 也用 Newey-West；`freeze_sign` 改成真正的样本外约束（例如 holdout IC 下限 + 与 train 同号，且不得与 `min_rank_ic_mean` 重复）。
5. LLM 卡片只给 **训练窗** IC / 失败模式，禁止 `holdout_ic`。
6. 过闸前按已测次数做简单多重检验（至少报告 deflated IC 或 Bonferroni 后的有效门槛）。
7. 停循环，按新合同重评 4 条；不过就降为 screened/draft。

**好处：** 这是唯一能把「candidate」从实验室标签改成可对外口径的一步。没有它，后面所有搜索优化都是在错误样本上打磨噪声。预期 screened 明显变少；4 条里可能只剩 0～2 条。IC 数字多半下降——那说明旧 IC 含水，不是优化失败。

### P1 — 让下一轮搜索产出新结构（约 1 周，不依赖更长历史）

覆盖：#10 #11 #14 #15 #22 #24 #25，以及此前约定的目录耗尽方案。

1. catalog 空时：`fresh 3 + AST crossover 3 + mutate 1`，mutate 父因子禁止振幅。
2. 手加 15～25 条条件模板（成交量/换手分位 × 收益，而不是再堆 `std(high)/std(low)`）。
3. research 软通过补上换手；OOS 门槛从 0 提到与「至少不是全样本拟合」相当的弱正。
4. 弱 IC 也必须算库相关，或直接 cheap 拒绝、不准 `max_corr=0` 过闸。
5. `cheap_ic` 热路径也开；自动 `promote_screened` 改成显式开关，默认关。
6. 晋升队列按 production 的 NW ICIR 排。

**好处：** 循环不再空转振幅近亲。新结构进库的概率上升；screened 增速下降、算力浪费下降。**不保证**立刻再出现 candidate——P0 闸更严之后，本来就不该保证。

### P2 — 样本、中性、产品层（按需）

覆盖：#3 #12 #13 #16 #18 #27 #28 及工程项。

拉长历史（2019 或 2021 起）、点时行业、不可重叠持仓账、涨跌停掩码、中性顺序改成原始截面、候选库稳定后再做合成。工程：日志、seed、EvalService/sync 测试。

**好处：** holdout 从「22 个独立样本」变成可谈的稳健性；合成时不会把评测伪影当成因子。没有 P0，P2 是浪费。

---

## 严重度说明

| 级别 | 含义 |
|---|---|
| 高 | 会改变对 IC / `candidate` 标签的读法 |
| 中 | 抬高 screened 堆、评测偏松、或运维/复现风险 |
| 低 | 元数据与卫生，不直接改 IC 故事 |

每条结构：**现状 → 方案 → 好处**。好处写实际能换到的，不写「IC 提高」。

---

## 1. 数据与样本（高）

### 1.1 配置是 PIT，磁盘是最新成分快照

**现状。** `universe_policy.mode: pit`，`members.parquet` 只有 `20240102` 与 `20260630`，同一批 100 只。无 token，历史调样未落盘；已剔除成分无 K 线。4 条 IC 不能写成点时中证 100。

**方案。** 已有 `sync-universe`。配 token 后：

```text
qfactor sync-universe --start 20240101 --end 20260630
qfactor sync-data --start 20240101 --end 20260630 --source baostock
```

窗口须覆盖 2023-12 调样，否则 2024-01 的 asof 仍无效。sync 成功后把 `data_version.json` 的 `universe_mode` 写成 `pit`，与配置一致。无 token 时保持报错，禁止再盖最新名单。

**好处。** 日度掩码按半年调样分段常数，存活者偏差从「用今天的 100 只回看两年」降到「只在当时成分内」。IC 与分层第一次能叫中证 100。预期：后进指数的票在早期会被挖掉，部分振幅 IC 回落。

### 1.2 流通市值是估算

**现状。** `circ_mv ≈ 成交额/换手`。production 规模中性用的是这列。

**方案。** `_sync_with_adapter` 调用 `fetch_daily_basic`，写入点时 `circ_mv` / `turnover_rate`。估算值只作缺失回填，并在 report 的 `circ_mv_source` 区分 `vendor` / `estimated`。闸门：vendor 覆盖低于阈值则跳过规模中性并记失败，而不是静默用估算。

**好处。** 残差 IC 不再把「换手估市值」的噪声当成规模因子。中证 100 大票上绝对值变化可能不大，但口径可对外。与 1.1、1.4 一次做完。

### 1.3 样本短、截面窄、holdout 只有半年

**现状。** ~2.5 年、100 只；holdout ~111 日 / 22 个独立 5 日样本。`min_years_consistent: 2` 刚好卡 2024、2025。

**方案。** 截面保持中证 100，不扩全 A。时间上把起点扩到至少 2021（更好 2019），holdout 留最近 12 个月，train 至少覆盖 3 个完整年。`min_years_consistent` 提到 3。独立样本不够时 production 直接拒绝，而不是用 NW 把 22 个点「洗」过闸。

**好处。** 年份同号和 holdout 第一次有统计意义。不能变宽截面、不能消除量价因子在大盘上的拥挤。数据量和 Tushare 积分是成本；没有 PIT（1.1）拉长窗口会把快照偏差拉得更长，必须先 1.1。

### 1.4 Tushare 复权因子与日度估值未接入 sync

**现状。** 适配器有 `fetch_adj_factor` / `fetch_daily_basic`，sync 填 `adj_factor=1`。

**方案。** 同步循环内按代码拉取并 merge。Baostock 路径：OHLC 已是 qfq，则 **不要** 再乘 stub 因子；Tushare 未复权路径才用 `adj_factor` 算 `close_adj`。在 `data_version` 记录 `price_adjust: qfq_vendor | hfq_factor`。

**好处。** 有 token 之后真的能用上 token。避免「接了 Tushare 仍是估算市值」。与 1.2、5.3 同一补丁。

---

## 2. 评测合同（高 / 中）

挖掘与晋升现在评的不是同一张信号表。

| 环节 | Research（挖掘） | Production（晋升） |
|---|---|---|
| 样本 | train ≤ 20251231，末 5 日剔除 | holdout 20260105～20260623 |
| 信号 | `zscore(winsor(DSL))` | 同上，再 `rolling(5).mean()` |
| 收益 | 重叠 5 日 `close_adj` | 同样重叠 5 日 |
| ICIR | `nw_lags=0` | NW `lags=4` |
| OOS | 训练窗 WF，门槛 0 | `freeze_sign`：IC 均值 > 0 |
| 年份 | 可软跳过 | 必查，但是训练窗 IC |

### 2.1 挖掘与晋升信号定义不一致（高）

**方案。** 二选一，写进 spec 与 report：

- **推荐 A：** production 取消 `rolling(hold).mean()`。5 日持有只体现在收益 horizon 与成本，不改信号。挖掘与晋升同一 DSL。
- B：若业务就是 5 日平滑信号，则 research 也做同样 rolling，`signal_hold_days` 进入 `FactorSpec`。

禁止第三种：挖掘不平滑、晋升平滑。现有 4 条按选定定义重评。

**好处。** candidate 的 IC 就是矿工优化的那个量。选 A 通常会让当前 4 条 IC 下降（平滑被拿走）；那是去掉评测红利。选 B 则库里的因子语义变成「5 日均线信号」，下游必须按这个交易。

### 2.2 Research IC 重叠 5 日且无 Newey-West（高）

**方案。** research 与 production 都对日度 IC 做 `nw_lags = horizon-1`。research 的 `min_icir` 改成与 NW 同单位的较低门槛（不要继续用未调整的 1.0 年化）。报告同时保留原始 ICIR 作诊断。不改为不可重叠抽样也可以，NW 是最小补丁；若要更干净，可另加「每 H 日抽样」的诊断 IC。

**好处。** screened 从「自相关抬高的 ICIR」降到接近真实显著性。92 条会大批降为 draft。搜索不会再围着虚高 ICIR 打转。

### 2.3 `freeze_sign` 几乎不是额外样本外（高）

**方案。** `oos_mode: freeze_sign` 改为合取，且不得与主 IC 闸重复：

1. 方向冻结在 train（已有）；
2. holdout 定向 IC ≥ `min_oos_ic_mean`（例如 0.01，已配置却被 freeze_sign 短路）；
3. holdout NW ICIR ≥ `min_holdout_icir`（已有，保留）；
4. 删除「仅 IC>0」这条，或把它降为诊断字段，不进 `passed`。

**好处。** 「过了 OOS」第一次有独立信息。部分现在靠「均值略大于 0」的因子会留下。与 2.1、2.2 一起改闸，只重评一次。

### 2.4 Research OOS 门槛接近无效（中）

**方案。** 训练窗 walk-forward 保留作稳定性诊断，但 `min_oos_ic_mean` 提到弱正（如 0.005～0.01），`min_oos_pos_folds` ≥ 半数折。不要把 holdout 提前给 research——那是把 P0 的泄漏做大。

**好处。** 进 screened 的式子至少不是「训练窗里一折都撑不住」。对最终 candidate 影响小于 2.3；主要减少垃圾入库和后续晋升扫描量。

### 2.5 年份同号与主 IC 不在同一窗口（中）

**方案。** report 分列 `years_train` 与 `years_holdout`（holdout 不足一年就明示 `n_years_holdout=1` 并 **禁止** 用它满足 `min_years_consistent`）。production 硬条件只用 train 的多年同号 + holdout 定向不翻号（2.3）。拉长样本（1.3）后，再要求 holdout 所在年符号一致。

**好处。** 不再出现「2024–25 同号、过闸数字全是 2026H1」的误读。在扩样本前，这是披露问题；扩样本后才变成硬闸。

### 2.6 分层与成本不是可交易模拟（中）

**方案。** 保留现有重叠分层作 IC 诊断。另增一条 **不可重叠** 的 5 日再平衡账：T 日信号、T+1 成交、持有 5 日、换手按实际调仓、成本 10bp（可加简单冲击可选）。production 的 `require_cost_ls_positive` 改打这条账。涨跌停/停牌有数据再进掩码（4.2）。

**好处。** 成本闸对应一本能讲清的书。当前 4 条的纸面多空会变差。中证 100 上变化通常小于全 A，但仍能挡住日换手被低估的式子。

### 2.7 弱 IC 跳过库相关（中）

**方案。** 去掉 `|IC|` 过低则 `max_corr=0` 的短路。算力不够就：cheap_ic 直接 reject，根本不进 `apply_gate`。进闸的必须有真实 `max_corr`。

**好处。** 近亲不能靠「没算相关」混进 screened。与 3.4、P1 catalog 规则一起，振幅重复会明显下降。

### 2.8 Research 软通过不查换手和年份（中）

**方案。** 软通过仍可跳过年份（样本短时合理），但 **换手必须硬过** research 的 `max_daily_turnover`。年份等 1.3 后再变硬。

**好处。** screened 不再收日换手 >2 的噪声式子，晋升扫描更便宜。对 IC 故事影响小于闸门尺度统一。

### 2.9 中性顺序非标准（中）

**方案。** 评测改为：原始截面 → 行业/规模中性 → winsor → zscore。`compute()` 只返回原始（或仅 winsor），中性声明写在 eval 配置；`FactorSpec.neutralize` 要么删除要么真正读取。现有库全部重评（中性变了，IC 不可比）。

**好处。** 残差更接近「去掉行业和规模后的截面」。部分振幅因子可能本来就是波动/规模代理，中性后 IC 下降——应下降。工作量不小，放 P2，且必须在真 `circ_mv`（1.2）之后。

### 2.10 残差 IC 与入库顺序有关（中）

**方案。** 新 candidate 过闸后强制跑已有的 `rerank_candidates_by_resid`（现在 `refresh_production` 才跑）。首次过闸的 `resid_ic` 在 peers=0 时标记 `resid_ic_unconditional`，不得单独当「已正交」。

**好处。** 后进因子不会因为先进者「没被残差」而占坑。4 条里 3 条振幅会更容易互踢，只留相关最低的一条——这正是想要的。

---

## 3. 搜索与库结构（高 / 中）

### 3.1 组合目录耗尽，结构挤在振幅（高）

**方案。** 不加大 batch、不关 `max_per_skeleton`。`unused_compose=0` 且 batch=8 时：

- fresh LLM 假设 3（假设编译器，不是搜参）；
- AST crossover 3（已有 parser 树）；
- mutate 1，父因子禁止当前振幅骨架；
- 手写 15～25 条条件模板（量能分位、隔夜×日内、换手突变等）。

可选：骨架唯一性过闸后，才允许偶发的 LLM 目录扩写。停掉对 4 条振幅父因子的 mutate。

**好处。** 搜索从「振幅邻域随机游走」回到「新假设」。预期新 screened 的机制分布拉开；candidate 仍可能是 0——P0 闸更严之后这是正常的。继续空转的边际产出约为 0，这是停止空转的收益。

### 3.2 LLM 父因子卡片泄漏 holdout IC（高）

**方案。** `_factor_card` 只暴露 train 窗指标：`train_ic`、失败原因、骨架、换手。删除 `holdout_ic`；`resid_ic` / `cost_ls` 若来自 holdout 评测也禁止下发。卡片数据源改为 research 报告或 train 切片，不用 production summary。

**好处。** 变异不再对准 2026H1 调参。这是最便宜的高严重修复之一，不依赖 token。做完不能「提高」IC，只能降低过拟合；holdout 上现有 4 条的优势可能消失。

### 3.3 无多重检验修正（高）

**方案。** checkpoint 累计 `n_tested`。production 报告增加 `n_tested_to_date` 与 `ic_hurdle_adjusted`（例如 Bonferroni：`min_rank_ic_mean` 对应的双侧 p 按 n 放大；或 deflated ICIR）。硬闸可用「调整后仍过」或「未调整过闸但标记 `multiple_testing: fail` 不得 approved」。不要用它替代 holdout。

**好处。** 582 轮里挑出 0.03 IC 不再被默认为发现。人工 `promote` 到 approved 时有明确反对意见。预期 approved 长期保持个位数。

### 3.4 `cheap_ic` 只在冷启动生效（中）

**方案。** 热路径同样 `cheap_ic_min`（可略提高到 0.01）。未过则记 `cheap_rejects`，不写库。

**好处。** 全量评测次数下降，循环更快，screened 更少。与 2.7 配套。有漏杀强因子的风险，可用「cheap 用 train、阈值低于 research 闸」控制。

### 3.5 Checkpoint 截断去重记忆（中）

**方案。** hash 去重改持久化集合（sqlite 已有库则写入 `tested_hash` 表），不要 `[-5000:]`。lesson / banned skeleton 可仍截断，但 hash 不可丢。

**好处。** 长跑不再重复交同样的学费。对 IC 无直接影响，对 582 轮之后继续挖的成本有直接影响。

### 3.6 每轮自动晋升 screened（中）

**方案。** `run_production_graph` 的自动 `promote_screened` 默认关闭；CLI 显式 `qfactor library-promote-screened`。循环只产 screened。

**好处。** 研究与生产边界清晰；不会在闸门改到一半时把垃圾推成 candidate。与 2.1 一起，避免「每轮用错误信号定义晋升」。

### 3.7 晋升排序用年化 ICIR 而非 NW（中）

**方案。** 队列键改为 `abs(icir_nw) * max(oos_ic_mean, 0)`，与 production 闸同单位。

**好处。** 先复评的是更可能过 NW 闸的因子，减少错误的「先进者占残差坑」（2.10）。

### 3.8 catalog 与 spec 在只改状态时会漂（中）

**方案。** `update_status` 同步写 spec 的 `status`/`entry_gate`，并要求附带最新 report 或拒绝只改状态。candidate 的 `entry_gate` 必须为 `production`。

**好处。** 多样性、排序、人工阅读不再看过期 IC。低成本，避免运维误判。

---

## 4. 产品层

### 4.1 停在单因子入库

**方案。** 现在不要做合成。P0+P1 之后：下游只读 `USABLE_STATUSES`；提供 `library export --status candidate,approved`；合成作为独立模块（相关约束 + 残差加权），不进挖掘循环。`approved` 保持人工。

**好处。** 避免 92 条 screened 流进策略。合成在干净库上才有意义；现在做合成会把 3 条振幅加成一个更脆的振幅。

### 4.2 场次与成本偏实验室

**方案。** 与 2.6 同一本不可重叠账。数据侧：Tushare/Baostock 能拿到则加停牌、涨跌停、ST 标记，因子与收益在不可交易日打 NaN。前复权：`data_version` 钉死 `adj_asof`；再 sync 必须换 `data_version` 并触发全库重评，禁止静默改历史价。

**好处。** 分层不再包含涨停买不进。中证 100 上命中率低于小盘，但仍能去掉个别日的虚假收益。qfq 钉版本后，昨天的 IC 明天还能对上同一份数。

---

## 5. 数据工程（中）

### 5.1 行业不是点时

**方案。** 用 Tushare 行业成分历史或申万点时接口扩成 `(trade_date, ts_code) → industry`。没有历史则明确 `industry_mode: snapshot`，与 universe 一样不要假装 PIT。中证 100 行业迁移频率低，优先级低于 1.1。

**好处。** 行业中性在调样/改类附近不再用未来标签。对大盘量价因子的 IC 影响通常小于成分 PIT。

### 5.2 质检过薄

**方案。** `check_daily_panel` 增加：成分日覆盖（PIT 下每日约 100）、`circ_mv` vendor 覆盖、`adj_factor` 非全 1（Tushare 路径）、OHLC 逻辑、换手缺失率。失败则 sync 非 0 退出。

**好处。** 坏数据进不了评测。防止「有 60100 行」被当成「数据正确」。

### 5.3 Baostock qfq + stub 复权因子

**方案。** 见 1.4。按源分支：vendor-qfq 不再二次复权。DSL 文档写清：振幅用 OHLC 时必须同一复权口径。

**好处。** 当前盘面行为不变，未来混源时不会把 `close` 与 `close_adj` 当两个信息。

### 5.4 Akshare 交易日历来自指数日线

**方案。** 日历只信任 Tushare `trade_cal` 或 Baostock 交易日。Akshare 路径禁止用指数日线冒充 `is_open`；没有日历则失败，不 fallback。

**好处。** 节假日错位导致的收益/切分错误消失。仅在走 Akshare fallback 时有感。

---

## 6. 工程与复现（中 / 低）

### 6.1 无结构化日志，失败常被吞掉（中）

**方案。** 标准 `logging`；`except Exception: pass` 改为记录 warning 并计入 report（中性跳过、peer 加载失败、DB upsert 失败）。DB 失败应让 `save_factor_files` 非静默。

**好处。** 循环跑歪时能定位。不改 IC，改可维护性；避免「catalog 有、sqlite 没有」这类幽灵状态。

### 6.2 无全局随机种子（中）

**方案。** `project.yaml` 增加 `production.seed`；`decide` 里 `random.seed(seed + iteration)`。LLM 仍非决定性，但 compose/crossover/抽样可复现。

**好处。** 同一 checkpoint 可重放同一批 DSL（LLM 槽除外）。审计和对比「改闸前后」需要这个。

### 6.3 核心生产路径测试缺口（中）

**方案。** 用小型合成面板测：`evaluate_panel` 的 split/NW/rolling 开关、`promote_screened` 状态机、`sync` 缺列行为。不需要连真实 Tushare。

**好处。** 2.1/2.2 这类合同改动不会无声回退。P0 改闸的配套，不是独立项目。

### 6.4 表达式 hash 无代数归一（低）

**方案。** hash 前对 `add/mul` 参数排序，`div(a,b)` 保持有序。不必上完整 CAS。

**好处。** 少存交换律重复。对当前振幅候选影响很小。

### 6.5 `operators.yaml` 不进运行时（低）

**方案。** validate 从 yaml 加载白名单，prompt 引用同一份。或删 yaml，只保留代码为单一来源。

**好处。** LLM 不再被文档允许、parser 拒绝的算子卡住。减少无效 round。

### 6.6 相关降级只看单边 `max_corr_with`（低）

**方案。** 对 candidate 对做对称相关矩阵，超阈保留 `|IC|` 高的一侧。

**好处。** 与 2.10 一起保证振幅双胞胎只留一个。

### 6.7 Web 任务状态在进程内存（低）

**方案。** 不修，或把 job 指针写到 `runs/`。CLI 才是生产入口。

**好处。** 几乎无研究收益。标明即可，避免把时间花在 UI。

---

## 7. 现有 4 条 candidate 的使用边界（优化前后）

**现在（未优化）合法用法：** 内部练合成；写明快照样本、估算市值、2026H1 holdout、production 含 5 日滚动。振幅最多留 1 条 + 隔夜。

**现在不合法：** 称点时中证 100、称可上线、外推 IC、把 screened 进策略。

**P0 之后：** 4 条必须全量重评。通过的才保留 candidate，并写 `data_version` + 信号定义。不通过的降级。不要把旧 IC 抄到新报告。

**P0+P1 之后：** 库的价值是「小而可复述」，不是「条数」。下游合成只读留下的 0～N 条。

---

## 8. 清单总表（缺点 / 方案 / 好处）

| # | 严重度 | 缺点 | 优化方案 | 优化后的好处 |
|---|---|---|---|---|
| 1 | 高 | 配置 PIT，磁盘快照 100 只 | token + `sync-universe`/`sync-data` 并集 | 可声称点时中证 100；去掉后进成分的回看偏差 |
| 2 | 高 | `circ_mv` 估算 | sync 接入 `daily_basic` | 规模中性口径正确；残差少一层换手噪声 |
| 3 | 高 | 样本 2.5 年、holdout 半年 | 先 PIT，再把历史扩到 ≥2021，holdout 12 个月 | 年份闸和 OOS 有统计意义 |
| 4 | 高 | adj/估值 API 未调用 | sync 真正 fetch；按源分支复权 | token 能换成真数据，而不是仍填 1 |
| 5 | 高 | 挖掘与晋升信号不同 | 取消晋升滚动，或两边都滚并写入 spec | candidate IC 与搜索目标一致 |
| 6 | 高 | Research 重叠 IC 无 NW | 两闸都 NW；门槛改同单位 | screened 大幅缩水，少追虚高 ICIR |
| 7 | 高 | freeze_sign ≈ IC>0 | OOS 用 holdout IC/ICIR 下限，删重复检查 | 「过 OOS」有独立信息 |
| 8 | 高 | LLM 卡片含 holdout IC | 卡片只给 train 指标 | 变异不再瞄 2026H1 |
| 9 | 高 | 无多重检验 | 累计 n_tested + 调整门槛/标记 | 0.03 IC 不再默认算发现 |
| 10 | 高 | catalog 耗尽、全是振幅 | fresh+crossover+非振幅 mutate+手写模板 | 有机会出新结构；停止空转 |
| 11 | 中 | Research OOS 门槛 0 | 训练窗 WF 弱正 + 半数折 | 少把全样本拟合送进 screened |
| 12 | 中 | 年份用 train、IC 用 holdout | 分列披露；扩样本后再硬约束 holdout 年 | 避免「两年同号其实只过了半年」 |
| 13 | 中 | 分层/成本是重叠均值 | 另做不可重叠 5 日账打成本闸 | 成本闸对应可讲述的交易 |
| 14 | 中 | 弱 IC 相关记 0 | 禁止短路；cheap 拒绝或必算相关 | 近亲无法混过相关闸 |
| 15 | 中 | 软通过不查换手 | research 换手改硬闸 | screened 更干净、晋升更便宜 |
| 16 | 中 | 先 zscore 再中性 | 先中性再 winsor/zscore（P2） | 残差更像去暴露后的 alpha |
| 17 | 中 | 残差 IC 看入库顺序 | 过闸即 rerank；无 peer 时打标 | 振幅双胞胎互踢，只留一条 |
| 18 | 中 | 行业静态 | 点时行业或标明 snapshot | 改类附近中性不再用未来标签 |
| 19 | 中 | 质检过薄 | 覆盖率/市值/复权失败则 sync 失败 | 坏面板进不了评测 |
| 20 | 中 | qfq 再乘 adj=1 | 按数据源分支复权 | 混源时 OHLC 口径不裂 |
| 21 | 中 | Akshare 日历冒充交易日 | 只用交易所日历，否则失败 | 节假日错位消失 |
| 22 | 中 | cheap_ic 仅冷启动 | 热路径也预筛 | 少做全量评测，循环更快 |
| 23 | 中 | checkpoint 截断 hash | hash 持久化，不截断 | 长跑不重测旧式 |
| 24 | 中 | 每轮自动晋升 | 默认关，CLI 显式晋升 | 研究/生产边界清晰 |
| 25 | 中 | 晋升按 icir_ann 排 | 按 icir_nw 排 | 先评更可能过生产闸的因子 |
| 26 | 中 | 改 status 指标陈旧 | 改状态必须带 report；entry_gate 对齐 | 目录与多样性不读过期数 |
| 27 | 中 | 无合成；0 approved | P0 后再做导出+合成模块 | 下游不会吃到 92 条 screened |
| 28 | 中 | 10bp 纸面、无涨跌停 | 不可重叠账 + 停牌/涨跌停掩码 | 去掉不可买日的虚假收益 |
| 29 | 中 | 无日志、吞异常 | logging + 失败写入 report | 同步/评测幽灵失败可查 |
| 30 | 中 | 无 RNG seed | `seed + iteration` | compose 批次可重放 |
| 31 | 中 | 核心路径无测试 | 合成面板测闸门/sync/晋升 | P0 改合同不会无声回退 |
| 32 | 低 | hash 无交换律归一 | add/mul 参数排序再 hash | 少存重复式 |
| 33 | 低 | operators.yaml 不用 | 单一白名单来源 | 少 LLM 废 round |
| 34 | 低 | neutralize/entry_gate 名不副实 | 删除或真正读取；entry_gate=production | 元数据可当合同 |
| 35 | 低 | 降级只看单边相关 | 对称矩阵留高 \|IC\| | 双胞胎只留一侧 |
| 36 | 低 | Web job 仅内存 | 不修或写入 runs/ | 避免把时间花在 UI |
| 37 | 低 | qfq 再 sync 历史价漂 | `adj_asof` 钉版本，变则全库重评 | 昨天的 IC 对得上同一份数 |

---

## 9. 建议实施顺序（依赖）

```text
token
  → 1.1 PIT 成分 + 并集 K 线
  → 1.2 / 1.4 真市值与复权（同一 sync）
  → 5.2 质检加严
  → 2.1 信号定义对齐 + 3.2 去掉 holdout 泄漏 + 3.6 关掉自动晋升
  → 2.2 / 2.3 / 2.7 改闸
  → 重评 4 条 candidate（可能清掉大部分）
  → 3.1 / 3.4 恢复结构搜索
  → 1.3 再考虑拉长历史
  → 2.6 / 4.2 / 2.9 / 4.1 账本、掩码、中性顺序、合成
```

工程项（6.1～6.3、6.2 seed）可与 P0 并行，不阻塞 token。

---

## 10. 产品边界（不是漏洞，不优先「修」）

- 只做中证 100、日频、量价；不扩全 A / 基本面 / 分钟频来「救」IC
- 先单因子入库，合成放在库干净之后
- 半年调样 ⇒ PIT 掩码近似分段常数，仍要历史调样快照，不能用最新名单代替
- 不把 batch 加到 100、不关 `max_per_skeleton`、不用单因子 Sharpe 当硬闸
