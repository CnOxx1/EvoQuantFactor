# Copy to .env and fill in values
TUSHARE_TOKEN=
# Optional Tushare-compatible proxy (tinyshare). Do not commit the value.
TINYSHARE_TOKEN=
QFACTOR_TS_CLIENT=
# Required for qfactor loop / mine (factor production)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
QFACTOR_ROOT=
